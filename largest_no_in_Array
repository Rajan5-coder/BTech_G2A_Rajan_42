#include<stdio.h>
  int main(){
    int n,max,a[100];
    printf("Enter the size of Array: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
    printf("Enter the value of a[%d] : ",i);
    scanf("%d",&a[i]);
    } 
    max = a[0];
    for(int i=0;i<=7;i++){
        if(a[i]>max ){
            max = a[i];
        }
    }  
      printf("largest no. is =  %d ",max);

  return 0;
}  
