#include<stdio.h>
int main(){
   
    int i,n;
    printf("Enter the value of n : ");
    scanf("%d",&n);
    int f=1;
    for( i=1; i<=n; i++ ){
         f = f*i;
    }
       printf("%d",f);

return 0;
}
