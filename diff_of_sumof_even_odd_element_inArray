#include<stdio.h>
  int main(){
    
    int n,sum=0,sum1=0,a[5];
    for(int i=0;i<=4;i++){
    printf("Enter the value of a[%d] : ",i);
    scanf("%d",&a[i]);
    }
    for(int i=0;i<=4;i++){
        if(i%2==0){
            sum = sum + a[i];
        }else if(i%2!=0){
             sum1 = sum1 + a[i];
        }
    } 
    int diff = sum - sum1;
    printf("Difference = %d",diff);
  return 0;
}  
