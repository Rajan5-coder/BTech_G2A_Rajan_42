#include<stdio.h>
  int main(){

    int n,size,count=0,a[100];
    printf("Enter the size of array: ");
    scanf("%d",&size);
    printf("Enter the value of n: ");
    scanf("%d",&n);
    for(int i=0;i<size;i++){
    printf("Enter the value of a[%d] : ",i);
    scanf("%d",&a[i]);
    }
    for(int i=0;i<size;i++){
        if(a[i]>n){
            count++; 
        }
    } 
    printf("No. of Greater no. from %d is = %d ",n,count);
 return 0;
}     
